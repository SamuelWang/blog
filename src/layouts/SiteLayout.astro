---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

const isProduction = import.meta.env.PROD;
---

<html lang='en'>
  <head>
    <meta charset='utf-8' />
    <link rel='icon' type='image/svg+xml' href='/favicon.svg' />
    <link rel='icon' href='/favicon.ico' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <meta name='generator' content={Astro.generator} />
    <title>Samuel Wang</title>
  </head>
  <body class='bg-neutral-primary'>
    <Header />
    <main class='flex flex-col justify-between min-h-screen pt-22'>
      <!-- Content -->
      <div class='w-full mx-auto p-4 md:max-w-xl lg:max-w-2xl xl:max-w-3xl 2xl:max-w-4xl'>
        <slot />
      </div>
      <Footer />
    </main>
  </body>
</html>

<script is:inline src='https://cdn.jsdelivr.net/npm/flowbite@4.0.1/dist/flowbite.min.js'></script>

{
  isProduction && (
    <>
      <script>
        // Import the functions from the SDKs
        import { initializeApp } from 'firebase/app';
        import { getAnalytics } from 'firebase/analytics';

        // For Firebase JS SDK v7.20.0 and later, measurementId is optional
        const firebaseConfig = {
          apiKey: 'AIzaSyDitIZmbAEq-IRrxfqFfnJL0ndmy4fARno',
          authDomain: 'samuel-wang-s-blog.firebaseapp.com',
          projectId: 'samuel-wang-s-blog',
          storageBucket: 'samuel-wang-s-blog.firebasestorage.app',
          messagingSenderId: '759696888126',
          appId: '1:759696888126:web:be10b384e8ceaaf22c1c6f',
          measurementId: 'G-YQHJZVVGVD',
        };

        // Initialize Firebase
        const app = initializeApp(firebaseConfig);
        const analytics = getAnalytics(app);
      </script>
    </>
  )
}
